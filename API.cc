#include "lib/API.h"

bool GetGpio(gpio_t direction){ //STUB so it compiles
	return false;
}

void UpdateScreen(unsigned char *screen_buffer){ //STUB so it compiles
	return;
}

unsigned int GetTicks(){ //STUB so it compiles
	return 0;
}
